stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

build:
  image: node:16.13.0
  stage: build
  only:
    - main
  script:
    - version_number=$(date +"%Y.%m.%d.%H%M-$CI_BUILD_REF_NAME")
    - echo "REACT_APP_VERSION='$version_number'" > .env
    - npm i
    - npm run build
  artifacts:
      when: on_success
      name: portfolio-build
      paths:
        - build
      expire_in: 1 hour
  
deploy: 
  image: ubuntu
  stage: deploy
  only:
    - main
  variables:
    SERVER_NAME: "EOD-WEB-01"
    SERVICE_ACCOUNT: "deploy"
    SERVER_IP: "192.168.7.137"
  script:
    - apt update
    - apt install openssh-client -y
    - mkdir -p ~/.ssh
    - eval $(ssh-agent -s)
    - ssh-add <(echo "$DEPLOY_USER_SSH_KEY" | tr -d '\r')
    - ssh-keyscan $SERVER_IP >> ~/.ssh/known_hosts
    - scp -r ./build/* $SERVICE_ACCOUNT@$SERVER_IP:/usr/share/nginx/portfolio/